{
    "name": "passl-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "pnpm -r build",
        "test": "jest",
        "start:api": "pnpm --filter api start:dev",
        "start:worker": "pnpm --filter worker start:dev",
        "db:migrate": "pnpm --filter infra exec prisma migrate dev"
    },
    "devDependencies": {
        "typescript": "^5.1.3",
        "@types/node": "^20.3.1",
        "@types/jest": "^29.5.2",
        "jest": "^29.5.0",
        "ts-jest": "^29.1.0"
    }
}