{
    "name": "worker",
    "version": "1.0.0",
    "scripts": {
        "start:dev": "nest start --watch",
        "build": "nest build"
    },
    "dependencies": {
        "@nestjs/bullmq": "^10.0.0",
        "@nestjs/common": "^10.0.0",
        "@nestjs/config": "^3.0.0",
        "@nestjs/core": "^10.0.0",
        "@nestjs/schedule": "^6.1.1",
        "@passl/core": "workspace:*",
        "@passl/infra": "workspace:*",
        "bullmq": "^4.0.0"
    },
    "devDependencies": {
        "@nestjs/cli": "^10.0.0"
    }
}